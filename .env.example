# =============================================================================
# Antigravity Manager - Docker Environment Configuration
# =============================================================================
# Copy this file to .env and customize the values for your deployment.
# DO NOT commit .env files with real credentials to version control.
# =============================================================================

# -----------------------------------------------------------------------------
# Proxy Server Configuration
# -----------------------------------------------------------------------------

# Port for the proxy server (OpenAI/Anthropic compatible API)
PROXY_PORT=8045

# API key for authenticating requests to the proxy
# Generate a secure key: openssl rand -hex 32
PROXY_API_KEY=

# Request timeout in seconds (default: 120)
PROXY_REQUEST_TIMEOUT=120

# -----------------------------------------------------------------------------
# OAuth Configuration
# -----------------------------------------------------------------------------

# Host for OAuth redirect URI
# For local development: localhost
# For remote deployment: your-server.example.com
OAUTH_REDIRECT_HOST=localhost

# Port for OAuth callback server
OAUTH_REDIRECT_PORT=8888

# -----------------------------------------------------------------------------
# Upstream Proxy (Optional)
# -----------------------------------------------------------------------------
# Use this if your network requires a proxy to access the internet

# Enable upstream proxy (true/false)
UPSTREAM_PROXY_ENABLED=false

# Upstream proxy URL (e.g., http://proxy.example.com:8080)
UPSTREAM_PROXY_URL=

# -----------------------------------------------------------------------------
# Logging
# -----------------------------------------------------------------------------

# Log level: error, warn, info, debug
LOG_LEVEL=info

# -----------------------------------------------------------------------------
# VNC Configuration (GUI mode only)
# -----------------------------------------------------------------------------

# VNC port for remote display (GUI mode only)
VNC_PORT=5900

# X11 display for GUI mode
# DISPLAY=:0
